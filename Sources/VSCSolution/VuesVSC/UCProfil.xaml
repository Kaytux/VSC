<UserControl x:Class="VuesVSC.UCProfil"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VuesVSC"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <StackPanel Margin="20" Width="550" DockPanel.Dock="Right">
            <TextBlock Text="Succes :"/>
            <ListBox x:Name="lBox" DockPanel.Dock="Left" Background="{StaticResource GreyBackground}" ItemsSource="{Binding Utilisateur.achievement}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <DockPanel>
                            <TextBlock Text="{Binding Name}"/>
                            <TextBlock> : </TextBlock>
                            <TextBlock Text="{Binding Description}"/>
                            <TextBlock> (Achivied = 1 / Not = 0) : </TextBlock>
                            <TextBlock Text="{Binding Achivied}"/>
                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
        <Image Margin="40" MaxHeight="200" DockPanel.Dock="Top" Source="Images/icon.png"/>
        <TextBlock Margin="20" DockPanel.Dock="Top" Text="{Binding Utilisateur.Nom}"/>
        <StackPanel Margin="20" DockPanel.Dock="Right">
            <TextBlock Text="Note :"/>
            <TextBlock Text="Test..."/>
            <TextBlock Text="Test..."/>
            <TextBlock Text="Test..."/>
            <TextBlock Text="Test..."/>
            <TextBlock Text="Test..."/>
            <TextBlock Text="Test..."/>
        </StackPanel>
    </DockPanel>
</UserControl>
